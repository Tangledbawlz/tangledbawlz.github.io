
var name = window. prompt("Enter your name: "); alert("Your name is " + name);





let item = null;
let numbCrypt = null;




function cesearEncryption (item, numbCrypt)
{
    let encryption = item; //casting user input to a string value
    for(let i = 0; i < encryption.length; i++)
    {
       // = "item".charAt(i) + numbCrypt; //iterate through length of the users value and encrypt code

    }

    
    
}